# Evidencia RPA

Cada ejecucion exitosa de Playwright genera una captura PNG y un recibo JSON. Si el navegador falla, se crea un TXT con URL, payload y error para diagnostico.

La evidencia puede descargarse desde `Ejecuciones RPA` o `GET /api/rpa/runs/{id}/evidence`.
